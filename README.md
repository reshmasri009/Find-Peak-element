# Find-Peak-element
finding the peak element using binary search
#ALGORITHM
-take a mid value
-check if the mid value is less than the next value,if so the peak value lies right side
-else it is at left side
#COMPLEXITY
TIME=O(log n)
SPACE=O(1)
